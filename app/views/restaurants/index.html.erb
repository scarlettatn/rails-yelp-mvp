<h1>Restaurants</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :rating %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>
<%= link_to "See details", restaurants_path(@restaurants), class: "btn btn-primary"%>
